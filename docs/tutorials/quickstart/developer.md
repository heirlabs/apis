# Quickstart: Developer

## Goal
Generate your first contract via API.

## Steps
1. Create an API key in the Developer Portal.
2. Call `/api/v1/contracts/templates`.
3. Call `/api/v1/contracts/generate`.
4. Deploy with your preferred SDK.
